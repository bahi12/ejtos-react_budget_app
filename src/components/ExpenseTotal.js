import React, { useContext } from 'react';
//import { AppContext } from '../context/AppContext';
const ExpenseTotal = () => {
    return (
        <div className='alert alert-primary'>
            <span>Totl expense</span>
        </div>
    );
};
export default ExpenseTotal;
